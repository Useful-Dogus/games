{
  "name": "@dogus/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./tsconfig.base": "./tsconfig.base.json",
    "./tsconfig.next": "./tsconfig.next.json"
  },
  "scripts": {
    "build": "mkdir -p dist && cp tsconfig.base.json dist/tsconfig.base.json && cp tsconfig.next.json dist/tsconfig.next.json"
  }
}
